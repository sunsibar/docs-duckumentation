

# Running using Docker

Install Docker:

    make install-docker-ubuntu16
    
Run:

    make compile-docker

# Native run

First, install dependencies.

To compile natively:

	$ make compile-native
